# ROC + Calibration Evidence

Generated by:

```powershell
python ml/scripts/gen_curves.py
```

Source:

- checkpoint: `models/best_cervical.pt`
- split: `data/processed/split_test.csv`
- output: `web-react/public/samples/curves.json`
- evaluation path: same `CervicalDS + build_albu + evaluate(TTA=True)` path as `ml/scripts/eval_triage.py`

## Binary Triage Results

Definition: NILM = normal, LSIL/HSIL/SCC/KOIL = abnormal.

| Metric | Value |
|---|---:|
| n | 137 |
| AUROC | 0.963971 |
| ECE L1, 10 bins | 0.090679 |
| Brier score | 0.071015 |
| Threshold 0.5 confusion | TP 101 / TN 26 / FP 10 / FN 0 |

## Interpretation

- ROC supports the triage framing: abnormal cases are separable from NILM in the
  held-out public Herlev split.
- Calibration is now measured for binary triage, but **no temperature scaling or
  calibration tuning has been applied**.
- Do not claim the model is "well-calibrated" globally. Say: "binary-triage ECE
  is measured at 0.091 on the held-out split; calibration still needs external
  validation and tuning."

## What Not To Mix Up

- `models/test_metrics.json` still contains `ece=0.0` from the 5-class training
  pipeline and is explicitly marked as not properly computed.
- `curves.json` is the current calibration evidence for **binary triage only**.
